## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `pnpm test` passes (the CLI tests need Chromium: `pnpm exec playwright install chromium`)
- [ ] New bots link to vendor documentation and have the right category
- [ ] A changed check has a fixture under `site/fixture/` that proves it
- [ ] README or the recipe is updated if behaviour changed
